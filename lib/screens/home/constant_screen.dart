import 'package:flutter/material.dart';

class ConstantScreen extends StatelessWidget {
  const ConstantScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Hi');
  }
}
